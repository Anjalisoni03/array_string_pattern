#include <iostream>
using namespace std;
int main() {
    int x;
    cin>>x;
    if(x>0)
    {
        cout<<"no. is positive";
    }
    else if(x==0)
    {
        cout<<"no. is zero";
    }
    else
    {
        cout<<"no. is negative";
    }
    return 0;
}
